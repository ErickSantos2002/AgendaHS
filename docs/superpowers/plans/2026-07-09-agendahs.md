# AgendaHS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir o AgendaHS — um sistema de agendamento por disponibilidade (estilo Doodle): o dono cria eventos com várias datas, compartilha um link secreto, e as pessoas informam nome + as datas que topam; o dono acompanha tudo num painel privado com calendário.

**Architecture:** App Flask único, servindo tanto o painel privado (protegido por senha) quanto as páginas públicas de resposta, renderizando HTML com Jinja2 e persistindo em SQLite. Empacotado num Dockerfile único para deploy na VPS via EasyPanel.

**Tech Stack:** Python 3, Flask, Jinja2, SQLite (stdlib `sqlite3`), Gunicorn, pytest.

## Global Constraints

- Linguagem: Python 3 (base de imagem `python:3-slim`).
- Sem etapa de build de frontend: HTML server-side (Jinja2), CSS/JS estáticos servidos pelo Flask.
- Banco: SQLite em `/app/data/eventos.db`. O caminho do banco vem da env var `AGENDAHS_DB` (default `/app/data/eventos.db`); nos testes aponta para arquivo temporário.
- Senha do painel: env var `AGENDAHS_SENHA`. Se não estiver setada, o app deve falhar ao iniciar com mensagem clara (evita painel sem proteção).
- Segredo da sessão Flask: env var `AGENDAHS_SECRET_KEY` (default gerado só em dev).
- Schema criado automaticamente na primeira execução se as tabelas não existirem.
- Slug do evento: string curta secreta, gerada com `secrets.token_urlsafe`, 6–8 chars, apenas `[A-Za-z0-9_-]`.
- Timestamps: ISO 8601 (`datetime.now().isoformat(timespec="seconds")`).
- Datas candidatas: formato `YYYY-MM-DD`; horário opcional `HH:MM`.
- Commits frequentes, um por tarefa concluída.

---

## File Structure

```
AgendaHS/
├── app/
│   ├── __init__.py         # create_app(): cria/config o Flask, registra blueprints, garante schema
│   ├── db.py               # get_db(), close_db(), init_schema(), helpers de acesso
│   ├── slug.py             # gerar_slug()
│   ├── auth.py             # blueprint de login/logout + decorator login_required
│   ├── eventos.py          # blueprint do painel (criar evento, listar, detalhe, calendário)
│   ├── publico.py          # blueprint público (/e/<slug>, responder)
│   ├── templates/          # HTML Jinja2 (base, login, painel, criar, detalhe, publico, 404)
│   └── static/
│       ├── style.css       # estilo único, responsivo
│       └── app.js          # JS puro (copiar link, adicionar datas, marcar)
├── tests/
│   ├── conftest.py         # fixtures: app de teste + client + db temporário
│   ├── test_db.py
│   ├── test_auth.py
│   ├── test_eventos.py
│   └── test_publico.py
├── docs/superpowers/       # spec e este plano
├── requirements.txt
├── wsgi.py                 # ponto de entrada do Gunicorn (app = create_app())
├── Dockerfile
├── .dockerignore
├── .gitignore
└── README.md
```

Cada arquivo tem uma responsabilidade única. `db.py` isola todo acesso ao SQLite; os blueprints (`auth`, `eventos`, `publico`) separam as três áreas do sistema; `slug.py` isola a geração de identificadores.

---

## Task 1: Esqueleto do projeto + factory Flask

**Files:**
- Create: `requirements.txt`
- Create: `app/__init__.py`
- Create: `wsgi.py`
- Create: `.gitignore`
- Create: `tests/conftest.py`
- Create: `tests/test_smoke.py`

**Interfaces:**
- Produces: `create_app(config=None) -> Flask`. Aceita dict de override de config (usado nos testes para `TESTING`, `DB_PATH`, `SENHA`, `SECRET_KEY`). Registra uma rota `GET /health` que retorna `"ok"` com status 200.

- [ ] **Step 1: Escrever `requirements.txt`**

```
Flask==3.0.3
gunicorn==22.0.0
pytest==8.2.0
```

- [ ] **Step 2: Escrever o teste de smoke**

`tests/test_smoke.py`:
```python
def test_health_ok(client):
    resp = client.get("/health")
    assert resp.status_code == 200
    assert resp.data == b"ok"
```

- [ ] **Step 3: Escrever a fixture de teste**

`tests/conftest.py`:
```python
import tempfile, os
import pytest
from app import create_app


@pytest.fixture
def app():
    fd, path = tempfile.mkstemp(suffix=".db")
    os.close(fd)
    application = create_app({
        "TESTING": True,
        "DB_PATH": path,
        "SENHA": "segredo",
        "SECRET_KEY": "test-key",
    })
    yield application
    os.unlink(path)


@pytest.fixture
def client(app):
    return app.test_client()
```

- [ ] **Step 4: Rodar o teste e ver falhar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_smoke.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app'` (ainda não existe).

- [ ] **Step 5: Escrever a factory mínima**

`app/__init__.py`:
```python
import os
from flask import Flask


def create_app(config=None):
    app = Flask(__name__)
    app.config["DB_PATH"] = os.environ.get("AGENDAHS_DB", "/app/data/eventos.db")
    app.config["SENHA"] = os.environ.get("AGENDAHS_SENHA")
    app.config["SECRET_KEY"] = os.environ.get("AGENDAHS_SECRET_KEY", "dev-inseguro")
    if config:
        app.config.update(config)

    @app.route("/health")
    def health():
        return "ok"

    return app
```

`wsgi.py`:
```python
from app import create_app

app = create_app()
```

- [ ] **Step 6: Escrever `.gitignore`**

```
__pycache__/
*.pyc
.pytest_cache/
*.db
data/
.venv/
venv/
```

- [ ] **Step 7: Rodar o teste e ver passar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_smoke.py -v`
Expected: PASS.

- [ ] **Step 8: Commit**

```bash
cd ~/github/AgendaHS
git add requirements.txt app/__init__.py wsgi.py .gitignore tests/conftest.py tests/test_smoke.py
git commit -m "feat: esqueleto Flask com factory e rota de health"
```

---

## Task 2: Camada de banco (SQLite + schema)

**Files:**
- Create: `app/db.py`
- Modify: `app/__init__.py` (registrar `close_db` no teardown e chamar `init_schema` no startup)
- Test: `tests/test_db.py`

**Interfaces:**
- Consumes: `create_app` da Task 1; `app.config["DB_PATH"]`.
- Produces:
  - `get_db() -> sqlite3.Connection` — conexão por request (guardada em `flask.g`), com `row_factory = sqlite3.Row` e `PRAGMA foreign_keys = ON`.
  - `close_db(e=None) -> None` — fecha a conexão do request.
  - `init_schema(app) -> None` — cria as 4 tabelas se não existirem.
  - `init_app(app) -> None` — registra `close_db` no teardown e chama `init_schema`.

- [ ] **Step 1: Escrever os testes do banco**

`tests/test_db.py`:
```python
from app.db import get_db


def test_tabelas_existem(app):
    with app.app_context():
        db = get_db()
        nomes = {
            row["name"]
            for row in db.execute(
                "SELECT name FROM sqlite_master WHERE type='table'"
            )
        }
    assert {"evento", "evento_data", "participante", "participante_data"} <= nomes


def test_foreign_keys_ativo(app):
    with app.app_context():
        db = get_db()
        assert db.execute("PRAGMA foreign_keys").fetchone()[0] == 1
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_db.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.db'`.

- [ ] **Step 3: Implementar `app/db.py`**

```python
import sqlite3
from flask import g, current_app

SCHEMA = """
CREATE TABLE IF NOT EXISTS evento (
    id         TEXT PRIMARY KEY,
    titulo     TEXT NOT NULL,
    descricao  TEXT,
    criado_em  TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS evento_data (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    evento_id  TEXT NOT NULL REFERENCES evento(id) ON DELETE CASCADE,
    data       TEXT NOT NULL,
    horario    TEXT
);
CREATE TABLE IF NOT EXISTS participante (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    evento_id  TEXT NOT NULL REFERENCES evento(id) ON DELETE CASCADE,
    nome       TEXT NOT NULL,
    criado_em  TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS participante_data (
    participante_id INTEGER NOT NULL REFERENCES participante(id) ON DELETE CASCADE,
    evento_data_id  INTEGER NOT NULL REFERENCES evento_data(id) ON DELETE CASCADE,
    PRIMARY KEY (participante_id, evento_data_id)
);
"""


def get_db():
    if "db" not in g:
        g.db = sqlite3.connect(current_app.config["DB_PATH"])
        g.db.row_factory = sqlite3.Row
        g.db.execute("PRAGMA foreign_keys = ON")
    return g.db


def close_db(e=None):
    db = g.pop("db", None)
    if db is not None:
        db.close()


def init_schema(app):
    with app.app_context():
        db = get_db()
        db.executescript(SCHEMA)
        db.commit()


def init_app(app):
    app.teardown_appcontext(close_db)
    init_schema(app)
```

- [ ] **Step 4: Registrar o db na factory**

Em `app/__init__.py`, dentro de `create_app`, antes do `return app`:
```python
    from . import db
    db.init_app(app)
```
Garantir que a pasta do banco exista (útil para o volume `/app/data`). Logo após montar o config e antes de `db.init_app(app)`:
```python
    import os as _os
    _os.makedirs(_os.path.dirname(_os.path.abspath(app.config["DB_PATH"])), exist_ok=True)
```

- [ ] **Step 5: Rodar e ver passar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_db.py -v`
Expected: PASS (2 passed).

- [ ] **Step 6: Commit**

```bash
cd ~/github/AgendaHS
git add app/db.py app/__init__.py tests/test_db.py
git commit -m "feat: camada SQLite com schema das 4 tabelas"
```

---

## Task 3: Geração de slug

**Files:**
- Create: `app/slug.py`
- Test: `tests/test_slug.py`

**Interfaces:**
- Produces: `gerar_slug(n=8) -> str` — string com `n` caracteres do alfabeto `[A-Za-z0-9_-]`.

- [ ] **Step 1: Escrever o teste**

`tests/test_slug.py`:
```python
import re
from app.slug import gerar_slug


def test_slug_formato_e_tamanho():
    s = gerar_slug()
    assert len(s) == 8
    assert re.fullmatch(r"[A-Za-z0-9_-]+", s)


def test_slugs_diferentes():
    assert gerar_slug() != gerar_slug()
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_slug.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.slug'`.

- [ ] **Step 3: Implementar `app/slug.py`**

```python
import secrets
import string

_ALFABETO = string.ascii_letters + string.digits + "-_"


def gerar_slug(n=8):
    return "".join(secrets.choice(_ALFABETO) for _ in range(n))
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_slug.py -v`
Expected: PASS (2 passed).

- [ ] **Step 5: Commit**

```bash
cd ~/github/AgendaHS
git add app/slug.py tests/test_slug.py
git commit -m "feat: geração de slug secreto de evento"
```

---

## Task 4: Autenticação do painel (login/logout + login_required)

**Files:**
- Create: `app/auth.py`
- Create: `app/templates/base.html`
- Create: `app/templates/login.html`
- Modify: `app/__init__.py` (registrar blueprint auth; validar SENHA no startup)
- Test: `tests/test_auth.py`

**Interfaces:**
- Consumes: `app.config["SENHA"]`.
- Produces:
  - Blueprint `auth` com: `GET /login` (form), `POST /login` (confere senha → grava `session["logado"] = True`), `POST /logout`.
  - `login_required(view)` — decorator que redireciona para `/login` se `session.get("logado")` for falso.
  - Sessão marcada como logada com `session["logado"] = True`.

- [ ] **Step 1: Escrever os testes de auth**

`tests/test_auth.py`:
```python
def test_login_senha_certa(client):
    resp = client.post("/login", data={"senha": "segredo"}, follow_redirects=True)
    assert resp.status_code == 200
    assert "senha incorreta" not in resp.get_data(as_text=True).lower()


def test_login_senha_errada(client):
    resp = client.post("/login", data={"senha": "errada"})
    assert "senha incorreta" in resp.get_data(as_text=True).lower()


def test_rota_protegida_redireciona(client):
    resp = client.get("/painel")
    assert resp.status_code == 302
    assert "/login" in resp.headers["Location"]


def test_rota_protegida_ok_apos_login(client):
    client.post("/login", data={"senha": "segredo"})
    resp = client.get("/painel")
    assert resp.status_code == 200
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_auth.py -v`
Expected: FAIL — 404 nas rotas (blueprint ainda não existe).

- [ ] **Step 3: Criar `app/templates/base.html`**

```html
<!doctype html>
<html lang="pt-br">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% block title %}AgendaHS{% endblock %}</title>
  <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
  <main class="container">
    {% with mensagens = get_flashed_messages() %}
      {% if mensagens %}
        <div class="flash">{% for m in mensagens %}<p>{{ m }}</p>{% endfor %}</div>
      {% endif %}
    {% endwith %}
    {% block conteudo %}{% endblock %}
  </main>
  <script src="{{ url_for('static', filename='app.js') }}"></script>
</body>
</html>
```

- [ ] **Step 4: Criar `app/templates/login.html`**

```html
{% extends "base.html" %}
{% block title %}Entrar — AgendaHS{% endblock %}
{% block conteudo %}
<section class="card card-estreito">
  <h1>AgendaHS</h1>
  <p class="sub">Painel privado</p>
  {% if erro %}<p class="erro">Senha incorreta</p>{% endif %}
  <form method="post" action="{{ url_for('auth.login') }}">
    <label for="senha">Senha</label>
    <input type="password" id="senha" name="senha" autofocus required>
    <button type="submit">Entrar</button>
  </form>
</section>
{% endblock %}
```

- [ ] **Step 5: Implementar `app/auth.py`**

```python
from functools import wraps
from flask import (
    Blueprint, render_template, request, redirect, url_for, session, current_app
)

bp = Blueprint("auth", __name__)


def login_required(view):
    @wraps(view)
    def wrapped(*args, **kwargs):
        if not session.get("logado"):
            return redirect(url_for("auth.login"))
        return view(*args, **kwargs)
    return wrapped


@bp.route("/login", methods=["GET", "POST"])
def login():
    erro = False
    if request.method == "POST":
        if request.form.get("senha") == current_app.config["SENHA"]:
            session["logado"] = True
            return redirect(url_for("eventos.painel"))
        erro = True
    return render_template("login.html", erro=erro)


@bp.route("/logout", methods=["POST"])
def logout():
    session.clear()
    return redirect(url_for("auth.login"))
```

- [ ] **Step 6: Registrar o blueprint e uma rota `/painel` temporária**

Em `app/__init__.py`, dentro de `create_app`, após `db.init_app(app)`:
```python
    if not app.config.get("SENHA"):
        raise RuntimeError("AGENDAHS_SENHA não configurada — painel ficaria sem proteção")

    from . import auth
    app.register_blueprint(auth.bp)

    from .auth import login_required

    @app.route("/painel")
    @login_required
    def _painel_placeholder():
        return "painel ok"
```
Nota: essa rota `/painel` placeholder e seu endpoint serão substituídos pelo blueprint `eventos` na Task 5. O teste usa a URL `/painel`, que a Task 5 preserva.

- [ ] **Step 7: Ajustar `url_for('eventos.painel')`**

Como o blueprint `eventos` ainda não existe nesta task, troque temporariamente no `auth.py` o redirect de sucesso para:
```python
            return redirect(url_for("_painel_placeholder"))
```
(Reverter para `url_for("eventos.painel")` na Task 5.)

- [ ] **Step 8: Rodar e ver passar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_auth.py -v`
Expected: PASS (4 passed).

- [ ] **Step 9: Commit**

```bash
cd ~/github/AgendaHS
git add app/auth.py app/__init__.py app/templates/base.html app/templates/login.html tests/test_auth.py
git commit -m "feat: autenticação do painel com senha única e login_required"
```

---

## Task 5: Criar e listar eventos (painel)

**Files:**
- Create: `app/eventos.py`
- Create: `app/templates/painel.html`
- Create: `app/templates/evento_novo.html`
- Modify: `app/__init__.py` (remover placeholder `/painel`, registrar blueprint eventos)
- Modify: `app/auth.py` (redirect de sucesso volta a `eventos.painel`)
- Test: `tests/test_eventos.py`

**Interfaces:**
- Consumes: `get_db()` (Task 2), `gerar_slug()` (Task 3), `login_required` (Task 4).
- Produces:
  - Blueprint `eventos` registrado com `url_prefix=""`.
  - `GET /painel` → endpoint `eventos.painel` (lista eventos + calendário; calendário entra na Task 7).
  - `GET /eventos/novo` → endpoint `eventos.novo` (formulário).
  - `POST /eventos` → endpoint `eventos.criar` (cria evento + datas; redireciona para o detalhe).
  - `criar_evento(titulo, descricao, datas) -> str` (helper interno que retorna o slug), onde `datas` é lista de dicts `{"data": "YYYY-MM-DD", "horario": "HH:MM"|""}`.

- [ ] **Step 1: Escrever os testes**

`tests/test_eventos.py`:
```python
from app.db import get_db


def _login(client):
    client.post("/login", data={"senha": "segredo"})


def test_criar_evento_persiste(client, app):
    _login(client)
    resp = client.post("/eventos", data={
        "titulo": "Reunião",
        "descricao": "pauta X",
        "data": ["2026-08-05", "2026-08-06"],
        "horario": ["09:00", ""],
    }, follow_redirects=False)
    assert resp.status_code == 302
    with app.app_context():
        db = get_db()
        ev = db.execute("SELECT * FROM evento").fetchone()
        assert ev["titulo"] == "Reunião"
        n = db.execute(
            "SELECT COUNT(*) c FROM evento_data WHERE evento_id=?", (ev["id"],)
        ).fetchone()["c"]
        assert n == 2


def test_painel_lista_evento(client):
    _login(client)
    client.post("/eventos", data={
        "titulo": "Churrasco", "descricao": "",
        "data": ["2026-08-10"], "horario": [""],
    })
    resp = client.get("/painel")
    assert "Churrasco" in resp.get_data(as_text=True)


def test_criar_exige_login(client):
    resp = client.post("/eventos", data={"titulo": "X"})
    assert resp.status_code == 302
    assert "/login" in resp.headers["Location"]


def test_criar_exige_titulo(client):
    _login(client)
    resp = client.post("/eventos", data={
        "titulo": "", "descricao": "",
        "data": ["2026-08-10"], "horario": [""],
    })
    assert "título" in resp.get_data(as_text=True).lower()
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_eventos.py -v`
Expected: FAIL — rotas `/eventos` inexistentes.

- [ ] **Step 3: Implementar `app/eventos.py`**

```python
from datetime import datetime
from flask import (
    Blueprint, render_template, request, redirect, url_for
)
from .db import get_db
from .slug import gerar_slug
from .auth import login_required

bp = Blueprint("eventos", __name__)


def criar_evento(titulo, descricao, datas):
    db = get_db()
    slug = gerar_slug()
    db.execute(
        "INSERT INTO evento (id, titulo, descricao, criado_em) VALUES (?,?,?,?)",
        (slug, titulo, descricao or None,
         datetime.now().isoformat(timespec="seconds")),
    )
    for d in datas:
        db.execute(
            "INSERT INTO evento_data (evento_id, data, horario) VALUES (?,?,?)",
            (slug, d["data"], d["horario"] or None),
        )
    db.commit()
    return slug


@bp.route("/painel")
@login_required
def painel():
    db = get_db()
    eventos = db.execute("""
        SELECT e.id, e.titulo, e.criado_em,
               (SELECT COUNT(*) FROM evento_data d WHERE d.evento_id=e.id) n_datas,
               (SELECT COUNT(*) FROM participante p WHERE p.evento_id=e.id) n_respostas
        FROM evento e ORDER BY e.criado_em DESC
    """).fetchall()
    return render_template("painel.html", eventos=eventos)


@bp.route("/eventos/novo")
@login_required
def novo():
    return render_template("evento_novo.html")


@bp.route("/eventos", methods=["POST"])
@login_required
def criar():
    titulo = (request.form.get("titulo") or "").strip()
    descricao = (request.form.get("descricao") or "").strip()
    datas_raw = request.form.getlist("data")
    horarios_raw = request.form.getlist("horario")
    datas = [
        {"data": d.strip(), "horario": (horarios_raw[i] if i < len(horarios_raw) else "").strip()}
        for i, d in enumerate(datas_raw) if d.strip()
    ]
    if not titulo:
        return render_template("evento_novo.html", erro="Informe o título."), 400
    if not datas:
        return render_template("evento_novo.html", erro="Adicione ao menos uma data."), 400
    slug = criar_evento(titulo, descricao, datas)
    return redirect(url_for("eventos.detalhe", slug=slug))
```

Nota: `eventos.detalhe` é criado na Task 6. Para esta task passar, adicione um stub temporário no fim de `app/eventos.py`:
```python
@bp.route("/eventos/<slug>")
@login_required
def detalhe(slug):
    return f"detalhe {slug}"
```
(A Task 6 substitui o corpo deste stub.)

- [ ] **Step 4: Criar `app/templates/painel.html`**

```html
{% extends "base.html" %}
{% block title %}Painel — AgendaHS{% endblock %}
{% block conteudo %}
<header class="topo">
  <h1>AgendaHS</h1>
  <form method="post" action="{{ url_for('auth.logout') }}">
    <button class="link" type="submit">sair</button>
  </form>
</header>
<a class="botao" href="{{ url_for('eventos.novo') }}">+ Criar evento</a>
<section class="lista">
  <h2>Eventos</h2>
  {% if eventos %}
  <ul>
    {% for e in eventos %}
    <li>
      <a href="{{ url_for('eventos.detalhe', slug=e['id']) }}">{{ e["titulo"] }}</a>
      <span class="meta">{{ e["n_datas"] }} datas · {{ e["n_respostas"] }} respostas</span>
    </li>
    {% endfor %}
  </ul>
  {% else %}
  <p class="vazio">Nenhum evento ainda. Crie o primeiro!</p>
  {% endif %}
</section>
{% endblock %}
```

- [ ] **Step 5: Criar `app/templates/evento_novo.html`**

```html
{% extends "base.html" %}
{% block title %}Novo evento — AgendaHS{% endblock %}
{% block conteudo %}
<a class="voltar" href="{{ url_for('eventos.painel') }}">‹ voltar</a>
<section class="card">
  <h1>Criar evento</h1>
  {% if erro %}<p class="erro">{{ erro }}</p>{% endif %}
  <form method="post" action="{{ url_for('eventos.criar') }}">
    <label for="titulo">Título</label>
    <input id="titulo" name="titulo" required>

    <label for="descricao">Descrição (opcional)</label>
    <textarea id="descricao" name="descricao" rows="2"></textarea>

    <label>Datas candidatas</label>
    <div id="datas">
      <div class="linha-data">
        <input type="date" name="data" required>
        <input type="time" name="horario">
      </div>
    </div>
    <button type="button" id="add-data" class="link">+ adicionar data</button>

    <button type="submit">Criar evento</button>
  </form>
</section>
{% endblock %}
```

- [ ] **Step 6: Trocar placeholder por blueprint em `app/__init__.py`**

Remover a rota `_painel_placeholder` e seu registro. No lugar, após registrar `auth`:
```python
    from . import eventos
    app.register_blueprint(eventos.bp)
```
E em `app/auth.py`, reverter o redirect de sucesso do login para:
```python
            return redirect(url_for("eventos.painel"))
```

- [ ] **Step 7: Rodar e ver passar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_eventos.py tests/test_auth.py -v`
Expected: PASS (todos).

- [ ] **Step 8: Commit**

```bash
cd ~/github/AgendaHS
git add app/eventos.py app/__init__.py app/auth.py app/templates/painel.html app/templates/evento_novo.html tests/test_eventos.py
git commit -m "feat: criar e listar eventos no painel"
```

---

## Task 6: Detalhe do evento (resumo de disponibilidade)

**Files:**
- Modify: `app/eventos.py` (substituir o stub `detalhe`)
- Create: `app/templates/evento_detalhe.html`
- Test: `tests/test_eventos.py` (adicionar testes)

**Interfaces:**
- Consumes: `get_db()`, `login_required`, tabelas `evento`, `evento_data`, `participante`, `participante_data`.
- Produces:
  - `GET /eventos/<slug>` → endpoint `eventos.detalhe`, renderiza o resumo.
  - `resumo_evento(slug) -> dict|None` com chaves: `evento` (Row), `datas` (lista de Rows de `evento_data`), `participantes` (lista de dicts `{"nome": str, "disponibilidades": set[int]}` com os `evento_data_id`), `contagem` (dict `{evento_data_id: int}`), `melhor_data_id` (int|None). Retorna `None` se o evento não existir.

- [ ] **Step 1: Escrever os testes**

Adicionar em `tests/test_eventos.py`:
```python
def test_detalhe_mostra_contagem(client, app):
    _login(client)
    client.post("/eventos", data={
        "titulo": "Jogo", "descricao": "",
        "data": ["2026-08-01", "2026-08-02"], "horario": ["", ""],
    })
    with app.app_context():
        db = get_db()
        slug = db.execute("SELECT id FROM evento").fetchone()["id"]
        datas = db.execute(
            "SELECT id FROM evento_data WHERE evento_id=? ORDER BY id", (slug,)
        ).fetchall()
    # João topa as duas datas; Maria só a primeira
    client.post(f"/e/{slug}/responder", data={
        "nome": "João", "datas": [str(datas[0]["id"]), str(datas[1]["id"])],
    })
    client.post(f"/e/{slug}/responder", data={
        "nome": "Maria", "datas": [str(datas[0]["id"])],
    })
    resp = client.get(f"/eventos/{slug}")
    texto = resp.get_data(as_text=True)
    assert "João" in texto and "Maria" in texto


def test_detalhe_evento_inexistente_404(client):
    _login(client)
    resp = client.get("/eventos/naoexiste")
    assert resp.status_code == 404
```

Nota: estes testes dependem da rota pública `/e/<slug>/responder` da Task 7. Se executar as tasks em ordem, rode este teste ao final da Task 7. Marque este passo como "escrever agora, validar após Task 7".

- [ ] **Step 2: Implementar `resumo_evento` e a rota**

Em `app/eventos.py`, substituir o stub `detalhe` por:
```python
def resumo_evento(slug):
    db = get_db()
    evento = db.execute("SELECT * FROM evento WHERE id=?", (slug,)).fetchone()
    if evento is None:
        return None
    datas = db.execute(
        "SELECT * FROM evento_data WHERE evento_id=? ORDER BY data, horario", (slug,)
    ).fetchall()
    parts = db.execute(
        "SELECT * FROM participante WHERE evento_id=? ORDER BY criado_em", (slug,)
    ).fetchall()
    participantes = []
    contagem = {d["id"]: 0 for d in datas}
    for p in parts:
        ids = {
            r["evento_data_id"]
            for r in db.execute(
                "SELECT evento_data_id FROM participante_data WHERE participante_id=?",
                (p["id"],),
            )
        }
        for did in ids:
            if did in contagem:
                contagem[did] += 1
        participantes.append({"nome": p["nome"], "disponibilidades": ids})
    melhor_data_id = None
    if contagem:
        melhor = max(contagem.values())
        if melhor > 0:
            melhor_data_id = max(contagem, key=contagem.get)
    return {
        "evento": evento, "datas": datas, "participantes": participantes,
        "contagem": contagem, "melhor_data_id": melhor_data_id,
    }


@bp.route("/eventos/<slug>")
@login_required
def detalhe(slug):
    dados = resumo_evento(slug)
    if dados is None:
        return render_template("404.html"), 404
    link_publico = url_for("publico.responder_form", slug=slug, _external=True)
    return render_template("evento_detalhe.html", link_publico=link_publico, **dados)
```

Nota: `publico.responder_form` é criado na Task 7. `404.html` é criado na Task 8; até lá, o teste `test_detalhe_evento_inexistente_404` só passará após a Task 8 (ou crie um `404.html` mínimo agora). Para evitar dependência, crie já um `app/templates/404.html` mínimo neste passo:
```html
{% extends "base.html" %}
{% block conteudo %}<section class="card"><h1>Evento não encontrado</h1></section>{% endblock %}
```

- [ ] **Step 3: Criar `app/templates/evento_detalhe.html`**

```html
{% extends "base.html" %}
{% block title %}{{ evento["titulo"] }} — AgendaHS{% endblock %}
{% block conteudo %}
<a class="voltar" href="{{ url_for('eventos.painel') }}">‹ voltar</a>
<section class="card">
  <h1>{{ evento["titulo"] }}</h1>
  {% if evento["descricao"] %}<p class="sub">{{ evento["descricao"] }}</p>{% endif %}

  <div class="link-box">
    <span>Link para compartilhar:</span>
    <input id="link-publico" readonly value="{{ link_publico }}">
    <button type="button" class="copiar" data-alvo="link-publico">copiar</button>
  </div>

  <h2>Disponibilidade</h2>
  {% if participantes %}
  <div class="tabela-scroll">
    <table class="grade">
      <thead>
        <tr>
          <th>Pessoa</th>
          {% for d in datas %}
          <th class="{{ 'melhor' if d['id'] == melhor_data_id }}">
            {{ d["data"] }}{% if d["horario"] %}<br><small>{{ d["horario"] }}</small>{% endif %}
            <br><small>{{ contagem[d["id"]] }}</small>
          </th>
          {% endfor %}
        </tr>
      </thead>
      <tbody>
        {% for p in participantes %}
        <tr>
          <td>{{ p["nome"] }}</td>
          {% for d in datas %}
          <td class="{{ 'sim' if d['id'] in p['disponibilidades'] else 'nao' }}">
            {{ '✓' if d['id'] in p['disponibilidades'] else '' }}
          </td>
          {% endfor %}
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
  {% else %}
  <p class="vazio">Ninguém respondeu ainda. Compartilhe o link acima.</p>
  {% endif %}
</section>
{% endblock %}
```

- [ ] **Step 4: Rodar (parcial) e ver o teste de 404**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_eventos.py::test_detalhe_evento_inexistente_404 -v`
Expected: PASS. (O teste `test_detalhe_mostra_contagem` passa após a Task 7.)

- [ ] **Step 5: Commit**

```bash
cd ~/github/AgendaHS
git add app/eventos.py app/templates/evento_detalhe.html app/templates/404.html tests/test_eventos.py
git commit -m "feat: detalhe do evento com resumo de disponibilidade"
```

---

## Task 7: Página pública de resposta

**Files:**
- Create: `app/publico.py`
- Create: `app/templates/publico.html`
- Create: `app/templates/publico_ok.html`
- Modify: `app/__init__.py` (registrar blueprint publico)
- Test: `tests/test_publico.py`

**Interfaces:**
- Consumes: `get_db()`, tabelas `evento`, `evento_data`, `participante`, `participante_data`.
- Produces:
  - Blueprint `publico`.
  - `GET /e/<slug>` → endpoint `publico.responder_form` (form de resposta; 404 se slug não existe).
  - `POST /e/<slug>/responder` → endpoint `publico.responder` (cria/atualiza participante + disponibilidades; mostra confirmação).
  - Regra de atualização: se já existir `participante` com o mesmo `nome` (case-insensitive, trim) no evento, apaga as `participante_data` antigas e regrava, em vez de duplicar.

- [ ] **Step 1: Escrever os testes**

`tests/test_publico.py`:
```python
from app.db import get_db


def _criar_evento_com_datas(client, app):
    client.post("/login", data={"senha": "segredo"})
    client.post("/eventos", data={
        "titulo": "Reunião", "descricao": "",
        "data": ["2026-08-05", "2026-08-06"], "horario": ["", ""],
    })
    with app.app_context():
        db = get_db()
        slug = db.execute("SELECT id FROM evento").fetchone()["id"]
        datas = db.execute(
            "SELECT id FROM evento_data WHERE evento_id=? ORDER BY id", (slug,)
        ).fetchall()
    return slug, [d["id"] for d in datas]


def test_form_publico_abre_sem_login(client, app):
    slug, _ = _criar_evento_com_datas(client, app)
    c2 = app.test_client()  # sem sessão
    resp = c2.get(f"/e/{slug}")
    assert resp.status_code == 200
    assert "Reunião" in resp.get_data(as_text=True)


def test_form_publico_slug_inexistente_404(client):
    resp = client.get("/e/naoexiste")
    assert resp.status_code == 404


def test_responder_persiste(client, app):
    slug, datas = _criar_evento_com_datas(client, app)
    resp = client.post(f"/e/{slug}/responder", data={
        "nome": "  João  ", "datas": [str(datas[0])],
    })
    assert resp.status_code == 200
    with app.app_context():
        db = get_db()
        p = db.execute("SELECT * FROM participante WHERE evento_id=?", (slug,)).fetchone()
        assert p["nome"] == "João"
        n = db.execute(
            "SELECT COUNT(*) c FROM participante_data WHERE participante_id=?", (p["id"],)
        ).fetchone()["c"]
        assert n == 1


def test_responder_sem_nome_erro(client, app):
    slug, datas = _criar_evento_com_datas(client, app)
    resp = client.post(f"/e/{slug}/responder", data={"nome": "", "datas": [str(datas[0])]})
    assert "nome" in resp.get_data(as_text=True).lower()


def test_responder_zero_datas_permitido(client, app):
    slug, _ = _criar_evento_com_datas(client, app)
    resp = client.post(f"/e/{slug}/responder", data={"nome": "Ana"})
    assert resp.status_code == 200
    with app.app_context():
        db = get_db()
        p = db.execute("SELECT * FROM participante WHERE nome='Ana'").fetchone()
        assert p is not None
        n = db.execute(
            "SELECT COUNT(*) c FROM participante_data WHERE participante_id=?", (p["id"],)
        ).fetchone()["c"]
        assert n == 0


def test_responder_mesmo_nome_atualiza(client, app):
    slug, datas = _criar_evento_com_datas(client, app)
    client.post(f"/e/{slug}/responder", data={"nome": "Bia", "datas": [str(datas[0])]})
    client.post(f"/e/{slug}/responder", data={"nome": "bia", "datas": [str(datas[1])]})
    with app.app_context():
        db = get_db()
        n_p = db.execute(
            "SELECT COUNT(*) c FROM participante WHERE evento_id=?", (slug,)
        ).fetchone()["c"]
        assert n_p == 1
        p = db.execute("SELECT * FROM participante WHERE evento_id=?", (slug,)).fetchone()
        disp = db.execute(
            "SELECT evento_data_id FROM participante_data WHERE participante_id=?", (p["id"],)
        ).fetchall()
        assert [r["evento_data_id"] for r in disp] == [datas[1]]
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_publico.py -v`
Expected: FAIL — rotas públicas inexistentes.

- [ ] **Step 3: Implementar `app/publico.py`**

```python
from datetime import datetime
from flask import Blueprint, render_template, request, abort
from .db import get_db

bp = Blueprint("publico", __name__)


def _buscar_evento(slug):
    db = get_db()
    evento = db.execute("SELECT * FROM evento WHERE id=?", (slug,)).fetchone()
    if evento is None:
        abort(404)
    datas = db.execute(
        "SELECT * FROM evento_data WHERE evento_id=? ORDER BY data, horario", (slug,)
    ).fetchall()
    return evento, datas


@bp.route("/e/<slug>")
def responder_form(slug):
    evento, datas = _buscar_evento(slug)
    return render_template("publico.html", evento=evento, datas=datas)


@bp.route("/e/<slug>/responder", methods=["POST"])
def responder(slug):
    evento, datas = _buscar_evento(slug)
    nome = (request.form.get("nome") or "").strip()
    if not nome:
        return render_template(
            "publico.html", evento=evento, datas=datas, erro="Informe seu nome."
        ), 400
    ids_validos = {str(d["id"]) for d in datas}
    escolhidas = [int(x) for x in request.form.getlist("datas") if x in ids_validos]

    db = get_db()
    existente = db.execute(
        "SELECT id FROM participante WHERE evento_id=? AND LOWER(nome)=LOWER(?)",
        (slug, nome),
    ).fetchone()
    if existente:
        pid = existente["id"]
        db.execute("DELETE FROM participante_data WHERE participante_id=?", (pid,))
    else:
        cur = db.execute(
            "INSERT INTO participante (evento_id, nome, criado_em) VALUES (?,?,?)",
            (slug, nome, datetime.now().isoformat(timespec="seconds")),
        )
        pid = cur.lastrowid
    for did in escolhidas:
        db.execute(
            "INSERT INTO participante_data (participante_id, evento_data_id) VALUES (?,?)",
            (pid, did),
        )
    db.commit()
    return render_template("publico_ok.html", evento=evento)
```

- [ ] **Step 4: Criar `app/templates/publico.html`**

```html
{% extends "base.html" %}
{% block title %}{{ evento["titulo"] }}{% endblock %}
{% block conteudo %}
<section class="card">
  <h1>{{ evento["titulo"] }}</h1>
  {% if evento["descricao"] %}<p class="sub">{{ evento["descricao"] }}</p>{% endif %}
  {% if erro %}<p class="erro">{{ erro }}</p>{% endif %}
  <form method="post" action="{{ url_for('publico.responder', slug=evento['id']) }}">
    <label for="nome">Seu nome</label>
    <input id="nome" name="nome" required autofocus>

    <p class="label">Quais datas você consegue?</p>
    <ul class="datas-check">
      {% for d in datas %}
      <li>
        <label>
          <input type="checkbox" name="datas" value="{{ d['id'] }}">
          {{ d["data"] }}{% if d["horario"] %} · {{ d["horario"] }}{% endif %}
        </label>
      </li>
      {% endfor %}
    </ul>
    <button type="submit">Enviar resposta</button>
  </form>
</section>
{% endblock %}
```

- [ ] **Step 5: Criar `app/templates/publico_ok.html`**

```html
{% extends "base.html" %}
{% block title %}Obrigado!{% endblock %}
{% block conteudo %}
<section class="card card-estreito centro">
  <h1>Valeu! 🎉</h1>
  <p>Sua resposta para <strong>{{ evento["titulo"] }}</strong> foi registrada.</p>
  <p class="sub">Pode fechar esta página.</p>
</section>
{% endblock %}
```

- [ ] **Step 6: Registrar o blueprint em `app/__init__.py`**

Após registrar `eventos`:
```python
    from . import publico
    app.register_blueprint(publico.bp)
```

- [ ] **Step 7: Rodar tudo e ver passar**

Run: `cd ~/github/AgendaHS && python -m pytest -v`
Expected: PASS em todos, incluindo `test_detalhe_mostra_contagem` da Task 6.

- [ ] **Step 8: Commit**

```bash
cd ~/github/AgendaHS
git add app/publico.py app/__init__.py app/templates/publico.html app/templates/publico_ok.html tests/test_publico.py
git commit -m "feat: página pública de resposta com upsert por nome"
```

---

## Task 8: Calendário no painel + página 404

**Files:**
- Modify: `app/eventos.py` (montar dados do calendário na rota `painel`)
- Modify: `app/templates/painel.html` (renderizar o calendário)
- Modify: `app/__init__.py` (handler global de 404)
- Test: `tests/test_eventos.py` (teste do calendário)

**Interfaces:**
- Consumes: `get_db()`, stdlib `calendar` e `datetime`.
- Produces:
  - `dados_calendario(ano, mes) -> dict` com: `ano`, `mes`, `semanas` (lista de semanas; cada semana é lista de 7 dicts `{"dia": int|None, "eventos": list[Row]}`), `mes_anterior` (`(ano, mes)`), `mes_proximo` (`(ano, mes)`), `nome_mes` (str).
  - `painel` aceita query params `?ano=&mes=`; sem eles, usa mês corrente.
  - Handler `@app.errorhandler(404)` que renderiza `404.html`.

- [ ] **Step 1: Escrever o teste**

Adicionar em `tests/test_eventos.py`:
```python
def test_calendario_marca_dia_do_evento(client):
    _login(client)
    client.post("/eventos", data={
        "titulo": "Festa", "descricao": "",
        "data": ["2026-08-15"], "horario": [""],
    })
    resp = client.get("/painel?ano=2026&mes=8")
    texto = resp.get_data(as_text=True)
    assert "tem-evento" in texto  # classe CSS marcando o dia 15
    assert "Festa" in texto


def test_404_pagina_amigavel(client):
    resp = client.get("/rota/que/nao/existe")
    assert resp.status_code == 404
    assert "não encontrad" in resp.get_data(as_text=True).lower()
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd ~/github/AgendaHS && python -m pytest tests/test_eventos.py::test_calendario_marca_dia_do_evento tests/test_eventos.py::test_404_pagina_amigavel -v`
Expected: FAIL.

- [ ] **Step 3: Implementar `dados_calendario` e ajustar `painel`**

Em `app/eventos.py`, adicionar imports no topo:
```python
import calendar
from datetime import datetime, date
```
Adicionar a função:
```python
_MESES = ["", "Janeiro", "Fevereiro", "Março", "Abril", "Maio", "Junho",
          "Julho", "Agosto", "Setembro", "Outubro", "Novembro", "Dezembro"]


def dados_calendario(ano, mes):
    db = get_db()
    prefixo = f"{ano:04d}-{mes:02d}-"
    linhas = db.execute("""
        SELECT d.data AS data, e.id AS evento_id, e.titulo AS titulo
        FROM evento_data d JOIN evento e ON e.id = d.evento_id
        WHERE d.data LIKE ?
    """, (prefixo + "%",)).fetchall()
    por_dia = {}
    for row in linhas:
        dia = int(row["data"][8:10])
        por_dia.setdefault(dia, []).append(row)

    cal = calendar.Calendar(firstweekday=6)  # domingo primeiro
    semanas = []
    for semana in cal.monthdayscalendar(ano, mes):
        semanas.append([
            {"dia": (d or None), "eventos": por_dia.get(d, []) if d else []}
            for d in semana
        ])
    mes_anterior = (ano - 1, 12) if mes == 1 else (ano, mes - 1)
    mes_proximo = (ano + 1, 1) if mes == 12 else (ano, mes + 1)
    return {
        "ano": ano, "mes": mes, "semanas": semanas,
        "mes_anterior": mes_anterior, "mes_proximo": mes_proximo,
        "nome_mes": _MESES[mes],
    }
```
Substituir a função `painel` por:
```python
@bp.route("/painel")
@login_required
def painel():
    hoje = date.today()
    try:
        ano = int(request.args.get("ano", hoje.year))
        mes = int(request.args.get("mes", hoje.month))
        if not (1 <= mes <= 12):
            raise ValueError
    except (TypeError, ValueError):
        ano, mes = hoje.year, hoje.month
    db = get_db()
    eventos = db.execute("""
        SELECT e.id, e.titulo, e.criado_em,
               (SELECT COUNT(*) FROM evento_data d WHERE d.evento_id=e.id) n_datas,
               (SELECT COUNT(*) FROM participante p WHERE p.evento_id=e.id) n_respostas
        FROM evento e ORDER BY e.criado_em DESC
    """).fetchall()
    cal = dados_calendario(ano, mes)
    return render_template("painel.html", eventos=eventos, cal=cal)
```

- [ ] **Step 4: Renderizar o calendário em `app/templates/painel.html`**

Inserir, logo após o botão "+ Criar evento" e antes da `<section class="lista">`:
```html
<section class="calendario">
  <div class="cal-topo">
    <a href="{{ url_for('eventos.painel', ano=cal['mes_anterior'][0], mes=cal['mes_anterior'][1]) }}">‹</a>
    <strong>{{ cal["nome_mes"] }} {{ cal["ano"] }}</strong>
    <a href="{{ url_for('eventos.painel', ano=cal['mes_proximo'][0], mes=cal['mes_proximo'][1]) }}">›</a>
  </div>
  <table class="cal">
    <thead>
      <tr><th>Dom</th><th>Seg</th><th>Ter</th><th>Qua</th><th>Qui</th><th>Sex</th><th>Sáb</th></tr>
    </thead>
    <tbody>
      {% for semana in cal["semanas"] %}
      <tr>
        {% for celula in semana %}
        <td class="{{ 'tem-evento' if celula['eventos'] else '' }}">
          {% if celula["dia"] %}
            <span class="num">{{ celula["dia"] }}</span>
            {% for ev in celula["eventos"] %}
              <a class="cal-ev" href="{{ url_for('eventos.detalhe', slug=ev['evento_id']) }}">{{ ev["titulo"] }}</a>
            {% endfor %}
          {% endif %}
        </td>
        {% endfor %}
      </tr>
      {% endfor %}
    </tbody>
  </table>
</section>
```

- [ ] **Step 5: Handler global de 404 em `app/__init__.py`**

Dentro de `create_app`, antes do `return app`:
```python
    from flask import render_template

    @app.errorhandler(404)
    def nao_encontrado(e):
        return render_template("404.html"), 404
```
Atualizar `app/templates/404.html` para o texto completo:
```html
{% extends "base.html" %}
{% block title %}Não encontrado — AgendaHS{% endblock %}
{% block conteudo %}
<section class="card card-estreito centro">
  <h1>Evento não encontrado</h1>
  <p class="sub">O link pode estar errado ou o evento foi removido.</p>
</section>
{% endblock %}
```

- [ ] **Step 6: Rodar tudo e ver passar**

Run: `cd ~/github/AgendaHS && python -m pytest -v`
Expected: PASS em todos.

- [ ] **Step 7: Commit**

```bash
cd ~/github/AgendaHS
git add app/eventos.py app/templates/painel.html app/templates/404.html app/__init__.py tests/test_eventos.py
git commit -m "feat: calendário no painel e página 404 amigável"
```

---

## Task 9: Estilo (CSS) e interações (JS)

**Files:**
- Create: `app/static/style.css`
- Create: `app/static/app.js`

**Interfaces:**
- Consumes: as classes já usadas nos templates (`container`, `card`, `card-estreito`, `botao`, `link`, `topo`, `voltar`, `flash`, `erro`, `sub`, `vazio`, `meta`, `link-box`, `copiar`, `tabela-scroll`, `grade`, `melhor`, `sim`, `nao`, `datas-check`, `label`, `calendario`, `cal`, `cal-topo`, `tem-evento`, `num`, `cal-ev`, `linha-data`, `centro`).
- Produces: um tema visual único e responsivo; JS para copiar link e adicionar linhas de data.

Esta task é visual — não tem teste automatizado. A verificação é manual (Task 10). Fazer o CSS caprichado, mobile-first, com boa hierarquia tipográfica e cores agradáveis (recomendo uma paleta escura suave com um dourado/âmbar de destaque, coerente com o gosto do Erick — ver `[[project-blackhole-theme]]`). Deve ser legível, com bom espaçamento, botões claros e a grade de disponibilidade com scroll horizontal no mobile.

- [ ] **Step 1: Escrever `app/static/style.css`**

Escrever um CSS completo cobrindo todas as classes listadas em "Consumes". Requisitos concretos:
- `:root` com variáveis de cor (fundo escuro, cartão levemente mais claro, texto claro, destaque âmbar `#e0a730`, verde para "sim").
- `.container { max-width: 860px; margin: 0 auto; padding: 24px; }`
- `.card` com fundo, borda arredondada, padding, sombra sutil; `.card-estreito { max-width: 420px; }`
- Inputs/textarea/button com estilo consistente, alvo de toque ≥ 44px de altura.
- `.grade th.melhor` destacado com a cor âmbar; `td.sim` com fundo verde e `td.nao` neutro.
- `.tabela-scroll { overflow-x: auto; }` para a grade rolar no mobile.
- Calendário: `table.cal` com células quadradas responsivas; `.tem-evento` com marca âmbar; `.cal-ev` como chip pequeno.
- Media query para telas estreitas (celular) ajustando paddings e fontes.

(O conteúdo exato do CSS é livre desde que cubra as classes; escrever à mão um arquivo coeso.)

- [ ] **Step 2: Escrever `app/static/app.js`**

```javascript
// Copiar link para a área de transferência
document.querySelectorAll(".copiar").forEach((btn) => {
  btn.addEventListener("click", () => {
    const alvo = document.getElementById(btn.dataset.alvo);
    if (!alvo) return;
    alvo.select();
    navigator.clipboard.writeText(alvo.value).then(() => {
      const original = btn.textContent;
      btn.textContent = "copiado!";
      setTimeout(() => (btn.textContent = original), 1500);
    });
  });
});

// Adicionar novas linhas de data no formulário de criar evento
const addBtn = document.getElementById("add-data");
if (addBtn) {
  addBtn.addEventListener("click", () => {
    const container = document.getElementById("datas");
    const linha = document.createElement("div");
    linha.className = "linha-data";
    linha.innerHTML =
      '<input type="date" name="data">' +
      '<input type="time" name="horario">';
    container.appendChild(linha);
  });
}
```

- [ ] **Step 3: Verificação rápida (os testes continuam passando)**

Run: `cd ~/github/AgendaHS && python -m pytest -v`
Expected: PASS (o CSS/JS não quebra nada; só confirma que os templates ainda renderizam).

- [ ] **Step 4: Commit**

```bash
cd ~/github/AgendaHS
git add app/static/style.css app/static/app.js
git commit -m "feat: estilo visual e interações (copiar link, adicionar datas)"
```

---

## Task 10: Empacotamento Docker + README + verificação manual

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`
- Create: `README.md`

**Interfaces:**
- Consumes: `wsgi.py` (`app = create_app()`), `requirements.txt`.
- Produces: imagem Docker que roda o app com Gunicorn na porta 8000, banco em `/app/data`.

- [ ] **Step 1: Escrever o `Dockerfile`**

```dockerfile
FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1 \
    AGENDAHS_DB=/app/data/eventos.db

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN mkdir -p /app/data
VOLUME ["/app/data"]

EXPOSE 8000

CMD ["gunicorn", "--bind", "0.0.0.0:8000", "--workers", "2", "wsgi:app"]
```

- [ ] **Step 2: Escrever `.dockerignore`**

```
__pycache__/
*.pyc
.pytest_cache/
.git/
.gitignore
tests/
*.db
data/
.venv/
venv/
docs/
```

- [ ] **Step 3: Escrever `README.md`**

Documentar: o que é o AgendaHS, como rodar local (`pip install -r requirements.txt`, setar `AGENDAHS_SENHA`, `flask --app wsgi run` ou `gunicorn`), como rodar os testes (`python -m pytest`), e o deploy no EasyPanel:
- criar app a partir do Dockerfile do repositório;
- setar env vars `AGENDAHS_SENHA` e `AGENDAHS_SECRET_KEY`;
- mapear volume persistente em `/app/data`;
- expor a porta 8000.

Conteúdo do README:
```markdown
# AgendaHS

Sistema simples de agendamento por disponibilidade (estilo Doodle). O dono cria
um evento com várias datas candidatas, compartilha um link secreto, e as pessoas
informam o nome e marcam as datas que topam. O dono acompanha as respostas num
painel privado com calendário.

## Stack
Flask + Jinja2 + SQLite, empacotado num Dockerfile único. Deploy na VPS via
EasyPanel.

## Rodar local
```bash
pip install -r requirements.txt
export AGENDAHS_SENHA="suasenha"
export AGENDAHS_SECRET_KEY="algo-aleatorio"
export AGENDAHS_DB="./data/eventos.db"
gunicorn --bind 0.0.0.0:8000 wsgi:app
# abra http://localhost:8000
```

## Testes
```bash
python -m pytest -v
```

## Deploy no EasyPanel
1. Novo app apontando para este repositório (build pelo Dockerfile).
2. Variáveis de ambiente:
   - `AGENDAHS_SENHA` — senha do painel.
   - `AGENDAHS_SECRET_KEY` — chave aleatória para a sessão.
3. Volume persistente montado em `/app/data`.
4. Porta interna 8000 (o EasyPanel cuida do proxy + HTTPS).
```

- [ ] **Step 4: Rodar a suíte de testes completa**

Run: `cd ~/github/AgendaHS && python -m pytest -v`
Expected: PASS em todos.

- [ ] **Step 5: Verificação manual local com Gunicorn**

Run:
```bash
cd ~/github/AgendaHS
python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
export AGENDAHS_SENHA="teste" AGENDAHS_SECRET_KEY="x" AGENDAHS_DB="./data/eventos.db"
gunicorn --bind 0.0.0.0:8000 wsgi:app
```
Expected: abrir `http://localhost:8000` → login com "teste" → criar um evento com 2 datas → abrir o link público numa aba anônima → responder → voltar ao painel e ver a resposta na grade e o dia marcado no calendário.

- [ ] **Step 6: (Opcional) Build da imagem Docker**

Run: `cd ~/github/AgendaHS && docker build -t agendahs .`
Expected: build conclui sem erro. (Rodar: `docker run --rm -p 8000:8000 -e AGENDAHS_SENHA=teste -e AGENDAHS_SECRET_KEY=x -v $PWD/data:/app/data agendahs`.)

- [ ] **Step 7: Commit**

```bash
cd ~/github/AgendaHS
git add Dockerfile .dockerignore README.md
git commit -m "feat: empacotamento Docker e documentação"
```

---

## Notas de execução

- **Ordem de dependência entre tasks:** algumas tasks criam stubs temporários para
  endpoints definidos em tasks seguintes (Task 4 → placeholder de painel; Task 5 →
  stub de `detalhe`; Task 6 referencia `publico.responder_form`). O teste
  `test_detalhe_mostra_contagem` (Task 6) só fica verde após a Task 7. Se executar
  em ordem, rode a suíte completa ao fim das Tasks 7, 8 e 10.
- **TDD:** todo passo de código começa por um teste que falha, exceto as tasks
  puramente visuais/infra (9 e 10), verificadas manualmente.
- **Commits:** um commit por task concluída (já embutido no último passo de cada).
